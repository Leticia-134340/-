 정처산기 시험에서 표에 값 넣을 때
아무 위치에 폴더 넣어서 텍스트 파일 넣고 cmd열기
명령어: sqlplus suystem/1234
create table 테이블명
(
널이름 자료형,
널이름 자료형,
...
필요한 널 값만큼 하기
);

(아마 직접 다 넣어야할 듯)

텍스트 파일 내용
insert into 테이블명 values(값, 값, 값, 값, 값);

다른이름 저장해서 ansi로 저장