VMnet0 Brdged 

gestpc<VMware<hostpc
어떤 네트워크를 쓸지 설정하는 단계 실제 컴과 렘이 직접적으로 연결
다른 컴에서 접속 가능

Bridged
안에서 밖으로 밖에서 안으로 쌍방향 가능
Host only
외부와 내부 단절 내부에서만 활동
NAT 보안통신
안에서 밖으로 통신 가능 반대는 불가능

통신망 안에 게스트 컴퓨터(가상 렘카)를 만들고 외부 네트워크를 가지고 온다
우분투- 개인 컴에 적합

slackware 가장 오래 됐지만 어려워서 혼자 쓰이지 않음

Redhat 서버 운영에 적합 
e.g.) centos
저장 폴더는 자유

하나로 파일
여러개로 파일

프로세스 - CPU
운영체제는 메모리가 기본 2기가 이상 있어야한다
3번째 종이 3묶음이 실행파일

마우스 키 다시 구현 컨트롤 알트

최소설치: 모든걸 명령어로
e.g.) 메모장 검색 - 메모장
메모장 명령- 메모장



먼 거리 컴퓨터 원격 접속용 putty

168.126.63.1 kt 공식 사이트?


consolas채가 문자 구분에 좋음
대소문자 구분 해야함

administrator 관리자 계정 		vs	 root
최고권한 계정은 아님
system계정이 따로 있다 


리눅스는 root
시스템에 버금가는 권한
who 현재 들어온 계정 정보 명령
~는 작업 위치 
틸드라는 기호로 현재 작업 중인 공간
대괄호 바깥의 특수문자는 계정 권한 표시
관리자: #
일반 유저: $
기본 명령어를 단독으로 넣거나 -으로 옵션값을 넣거나
리눅스 런레벨
0 종료
2 일반 유저
3 일반 유저
4 사용자 지정 모드
5 
6 리부트 재부팅
pwd 작업 폴더 위치

cd는 이동 경로 설정을 해야하는데 절대 경로와 상대 경로가 있다
내가 이동 경로를 다 적은 지금이 절대 경로 
상대 경로는 현재 위치를 기준으로 경로를 알려주는 것
cd . 은 현재 위치
cd . . 은 상위 폴더
상위폴더로 갔다가 원하는 폴더로 가는 것도 가능
e.g.) cd ../home/user1
-> 현재보다 상위 폴더로 갔다가 user1폴더로 이동